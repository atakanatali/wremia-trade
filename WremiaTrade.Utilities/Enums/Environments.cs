namespace WremiaTrade.Utilities.Enums;

/// <summary>
/// Enum for mapping the working environment of the application
/// </summary>
public enum Environments
{
    LOCAL = 1,
    TEST = 2,
    STAGING = 3,
    PRODUCTION = 4
}